# 项目协作说明

## 沟通偏好

- 与用户沟通时尽量使用中文。
- 只有命令、文件名、库名、报错信息、API、CLI、模型名、协议名等必须保留英文专有名词时才使用英文。

## 角色分工

- Codex 是主控，负责 `D:\Redbook_workflow` 当前仓库的主线代码修改、测试、输出文件检查、浏览器或 Chrome CDP 采集验证，以及合规边界维护。
- Claude Code 是副驾驶，适合做独立模块候选实现、设计审查、文案规则优化、测试遗漏检查和局部重构建议。
- Cursor 是人工编辑器，主要由用户查看 diff、微调规则、审阅文案和确认最终效果。

## 协作方式

- 默认不要让 Codex 和 Claude Code 同时在同一个目录自由修改同一批文件。
- 需要 Claude Code 参与时，优先通过共享任务文件交接，例如 `docs/claude-task.md` 和 `docs/claude-review.md`。
- Claude Code 可以在副本或 worktree 中实验，最终由 Codex 负责合并、验证和整理。
- 推荐流程：用户提出目标 -> Codex 拆任务并实现主线 -> Codex 写清 Claude 任务 -> Claude Code 输出建议或 patch -> Codex 合并并跑测试 -> 用户在 Cursor 中查看结果。

## 小红书建联边界

- 项目只做公开线索筛选、建联草稿生成、人工确认队列和后续信息整理。
- 不做陌生人首条私信的无人工确认自动发送。
- 只有在对方主动咨询、明确同意继续沟通，或人工确认已经发送并收到回复后，才用 AI 辅助整理姓氏、电话、纠纷问题等信息。
- 电话、姓名、对话内容等个人信息只保存在本地输出文件中，不写入调试日志。
- 遇到验证码、风控、登录失效或页面异常时，应暂停并提示人工处理，不绕过平台安全机制。

## 输出文件规则

- 测试和采集结果放在 `output/` 或明确命名的临时运行目录中，不要把大量结果散放在项目根目录。
- 临时测试输出完成后应清理，避免影响代码审阅和后续协作。
- 浏览器登录态目录如 `chrome-profile/` 只用于本机复用登录，不应提交到 Git。
